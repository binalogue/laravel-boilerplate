{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "php",
      "request": "launch",
      "name": "XDebug",
      "port": 9000,
      "pathMappings": {
        "/var/www": "${workspaceFolder}"
      }
    }
  ]
}
